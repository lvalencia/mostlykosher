<%= form_for(@admin_fan) do |f| %>
  <% if @admin_fan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_fan.errors.count, "error") %> prohibited this admin_fan from being saved:</h2>

      <ul>
      <% @admin_fan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
